# API Route Notes

Next.js App Router route handlers live in `src/app/api`. This folder is reserved for shared API helpers and documentation so backend code is easy to discover without moving framework-owned route files.
